Place production SSL files in this directory on the server.

Expected file names:
- fullchain.pem
- privkey.pem

Do not commit real certificate files.
Use the example files in this folder only as a format reference.
