services:
  backend:
    volumes:
      - static_data:/app/staticfiles

  frontend:
    volumes: []
    environment:
      - NODE_ENV=production
    command: sh -c "npm run build && npm run start -- -H 0.0.0.0 -p 3000"

volumes:
  static_data:

