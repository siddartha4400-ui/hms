// Temporary module declarations to silence the editor/TypeScript errors
// Install actual packages in the host environment (npm install react-bootstrap react-select react-icons)
// to get full types. These declarations are a stop-gap so the editor stops complaining
// when the libraries are only installed inside the dev container.

declare module "react-bootstrap";
declare module "react-select";
declare module "react-icons";
